using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WinScreenDebug : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        StaticData.winningTeamID = 2;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
